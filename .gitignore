.env
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.DS_Store
# Trained model artifacts — users train their own. Ignore the artifacts,
# never the `models/` package: `models/*.py` is bot source and a clean
# clone fails at `from models import ...` without it.
*.pkl
*.joblib
pnl*.db
pnl.json
logs/
